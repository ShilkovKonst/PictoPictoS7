<div class="text-lg text-center font-bold p-2 border-b">
  <span class="font-normal">Nouvelle note du dossier</span> 
  <a class="hover:text-pred trasition duration-150 ease-in-out"
    href="{{ path( 'therapist_patients_get_one', { code: code } ) }}">
    {{ code }}
  </a>
</div>

{{ form_start(noteForm, {
'attr': {
'class': 'flex flex-col md:gap-3 justify-between items-center w-full h-full p-2'
}
}) }}
<div class="w-full grid grid-cols-1 md:grid-cols-2 sm:gap-2 md:gap-4">
  {{ form_row(noteForm.estimation, {
  'attr': {
  'class': 'input-text z-10',
  },
  }) }}
  {{ form_row(noteForm.comment, {
  'attr': {
  'class': 'input-text z-10',
  },
  }) }}
</div>

<button type="submit" class="btn-b">Valider</button>
{{ form_end(noteForm) }}