<div class="">
  <p class="text-lg text-center font-bold p-2 border-b">
    <span class="font-normal">Desactivation du dossier</span>
    {{ code }}
  </p>
  <div>
    <p class=" text-base tracking-[0.25px] leading-relaxed m-1">
      Après la désactivation du dossier du patient, vous ne pourrez plus :
    </p>
    <ul class="list-inside list-disc text-base tracking-[0.25px] leading-relaxed m-1">
      <li><span class="font-bold">modifier les données</span> du patient</li>
      <li><span class="font-bold">prendre des notes</span> sur l'état du patient</li>
      <li><span class="font-bold">créer des sessions</span> de jeu pour le patient</li>
    </ul>
    <p class=" text-base tracking-[0.25px] leading-relaxed m-1">
      Toutes les fonctionnalités seront bloquées, seule la visualisation des données initiales de l'utilisateur
      désactivé sera possible.
    </p>
  </div>
</div>

{{ form_start(passwordForm, {
'attr': {
'class': 'flex flex-col md:gap-3 justify-center items-center w-full p-2'
}
}) }}
{{ form_row(passwordForm.password, {
'attr': {
'class': 'input-text z-10',
}
}) }}

<button type="submit" class="btn-b mb-6">Désactiver</button>
{{ form_end(passwordForm) }}